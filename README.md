# eashl
### Repo for compiling EASHL stats

This project requires the following
* `python` >= 3.11
* `poetry` installed, run `pip install poetry`

Steps to execute
1. cd into `src` directory
2. run `poetry shell` and `poetry install`
3. run `streamlit run eashl/main.py`
